

TBD

Common WAN/LAN IP
192.168.x.x

Loopback (local)
127.0.0.x


**COMMAND**

MAC:

```
Cmd + <space> term <enter> ifconfig <enter>
```

Window:

```
Win + R <enter> cmd <enter> ipconfig <enter>
```